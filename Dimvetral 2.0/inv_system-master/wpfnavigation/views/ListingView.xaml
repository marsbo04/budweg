<UserControl x:Class="wpfnavigation.views.ListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:wpfnavigation.views"
             xmlns:vms="clr-namespace:wpfnavigation.viewmodels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock HorizontalAlignment="Center" Margin="0,14,0,0" TextWrapping="Wrap" Text="sej_forside.png"
                   VerticalAlignment="Top" />
        <Button Command="{Binding NavigateToFirstViewCommand}" Content="go1" HorizontalAlignment="Left"
                Margin="-6,280,0,0" VerticalAlignment="Top" />
        <Button Command="{Binding NavigateToSecondViewCommand}" Content="go2" HorizontalAlignment="Left"
                Margin="275,280,0,0" VerticalAlignment="Top" />
        <DataGrid ItemsSource="{Binding DeliveryNotes}" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="75,53,75,123"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridCheckBoxColumn Header="Status" Binding="{Binding Status, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, UpdateSourceTrigger=PropertyChanged, StringFormat=yyyy-MM-dd HH:mm:ss}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>